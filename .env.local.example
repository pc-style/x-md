# Optional: Firecrawl scrape fallback when FxTwitter/syndication fail (local dev only)
# Do not set on Vercel Production for the public x-md deploy
FIRECRAWL_API_KEY=

# Converter cache (optional)
CACHE_TTL_SECONDS=3600
# CACHE_DISABLED=1
# CACHE_PERSIST=0
